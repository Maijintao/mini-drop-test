[minio]
# 凭证通过环境变量注入（config.py 优先读取环境变量）
# 生产环境必须设置: MINIO_ENDPOINT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_BUCKET
endpoint = minio:9000
access_key =
secret_key =
bucket = drop-data
secure = false

[apiserver]
url = http://apiserver:8191
